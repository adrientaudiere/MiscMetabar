% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rsearch_wrappers.R
\name{vs_fastx_uniques}
\alias{vs_fastx_uniques}
\title{Dereplicate sequences in a fastx file}
\usage{
vs_fastx_uniques(
  fastx_input,
  output_format = "fasta",
  fastx_output = NULL,
  minuniquesize = 1,
  strand = "plus",
  sizein = TRUE,
  sizeout = TRUE,
  relabel = NULL,
  relabel_sha1 = FALSE,
  fastq_qout_max = FALSE,
  fasta_width = 0,
  sample = NULL,
  vsearch_options = NULL,
  tmpdir = NULL
)
}
\arguments{
\item{fastx_input}{(required) Path to a fasta or fastq file (optionally
gzipped).}

\item{output_format}{(default \code{"fasta"}) Output format: \code{"fasta"} or
\code{"fastq"}.}

\item{fastx_output}{Path to the output file. A temporary file is used
when \code{NULL}.}

\item{minuniquesize}{(integer, default 1) Discard unique sequences with
an abundance lower than this value.}

\item{strand}{(default \code{"plus"}) Compare sequences on the \code{"plus"} strand
only or on \code{"both"} strands.}

\item{sizein}{(logical, default TRUE) Take \verb{;size=N} annotations of the
input into account.}

\item{sizeout}{(logical, default TRUE) Add \verb{;size=N} annotations to the
output.}

\item{relabel}{Relabel output sequences with this prefix followed by a
sequential number.}

\item{relabel_sha1}{(logical, default FALSE) Relabel output sequences
with their SHA1 digest.}

\item{fastq_qout_max}{(logical, default FALSE) Report the maximum quality
score in fastq output.}

\item{fasta_width}{(integer, default 0) Width of fasta sequence lines
(0 for no wrapping).}

\item{sample}{Optional sample name annotation added to output headers.}

\item{vsearch_options}{Additional vsearch command-line options.}

\item{tmpdir}{Directory for temporary files.}
}
\value{
\code{NULL} (invisibly). The dereplicated sequences are written to
\code{fastx_output}.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Wrapper of \code{\link[Rsearch:vs_fastx_uniques]{Rsearch::vs_fastx_uniques()}} (vsearch \code{--fastx_uniques}).
Dereplicate the reads of a fasta or fastq file: identical sequences are
merged and their abundances recorded as \verb{;size=N} annotations in the
output headers.
}
\examples{
\dontshow{if (requireNamespace("Rsearch", quietly = TRUE) && MiscMetabar::is_vsearch_installed()) withAutoprint(\{ # examplesIf}
vs_fastx_uniques(
  system.file("extdata", "ex_little.fasta", package = "MiscMetabar"),
  fastx_output = tempfile(fileext = ".fasta")
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[Rsearch:vs_fastx_uniques]{Rsearch::vs_fastx_uniques()}}, \code{\link[=vs_uchime_ref]{vs_uchime_ref()}}, \code{\link[=reads2otu]{reads2otu()}}
}
\author{
Adrien Taudière
}
