% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vsearch.R
\name{install_vsearch}
\alias{install_vsearch}
\title{Install vsearch binary}
\usage{
install_vsearch(
  version = "latest",
  path = tools::R_user_dir("MiscMetabar", "data"),
  force = FALSE
)
}
\arguments{
\item{version}{(default: "latest") The vsearch version to install
(e.g. \code{"2.30.5"}). Use \code{"latest"} to fetch the most recent release.}

\item{path}{(default: \code{tools::R_user_dir("MiscMetabar", "data")})
Directory where vsearch will be installed.}

\item{force}{(default: FALSE) If \code{TRUE}, re-download even if vsearch
is already installed.}
}
\value{
The path to the installed vsearch binary (invisibly).
}
\description{
Downloads and installs the vsearch binary from
\href{https://github.com/torognes/vsearch/releases}{GitHub} into the
MiscMetabar user data directory. This is especially useful on Windows
where vsearch is not available from a system package manager.

After installation, all MiscMetabar functions that use vsearch will
find the binary automatically via \code{\link[=find_vsearch]{find_vsearch()}}.
}
\examples{
\dontrun{
install_vsearch()
install_vsearch(version = "2.30.5")
}
}
\seealso{
\code{\link[=find_vsearch]{find_vsearch()}}, \code{\link[=is_vsearch_installed]{is_vsearch_installed()}}
}
\author{
Adrien Taudière
}
