% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strassoc_pq.R
\name{strassoc_pq}
\alias{strassoc_pq}
\title{Strength of species-group associations for a phyloseq object}
\usage{
strassoc_pq(
  physeq,
  fact,
  func = "IndVal.g",
  nboot_ci = NULL,
  alpha_ci = 0.05,
  ...
)
}
\arguments{
\item{physeq}{(required) a \code{\link[phyloseq]{phyloseq-class}} object obtained
using the \code{phyloseq} package.}

\item{fact}{(required) Name of the factor in \code{physeq@sam_data} defining the
groups of samples.}

\item{func}{(chr, default "IndVal.g") The association index to compute. See
\code{?indicspecies::strassoc()} for the full list. Common values are:
\itemize{
\item \code{"IndVal"} / \code{"IndVal.g"}: Dufrêne-Legendre indicator value (the \code{.g}
variant equalizes group sizes),
\item \code{"r"} / \code{"r.g"}: the point-biserial / phi coefficient of association,
\item \code{"A"}: specificity (positive predictive value) component,
\item \code{"B"}: fidelity (sensitivity) component.
}}

\item{nboot_ci}{(int, default NULL) Number of bootstrap replicates used to
estimate confidence intervals. When NULL (default), no bootstrap is
performed and a single estimate is returned per taxon and group.}

\item{alpha_ci}{(float, default 0.05) Error level for the bootstrap
confidence intervals (only used when \code{nboot_ci} is not NULL).}

\item{...}{Additional arguments passed on to \code{\link[indicspecies:strassoc]{indicspecies::strassoc()}}.}
}
\value{
\itemize{
\item When \code{nboot_ci} is NULL: a \code{tibble} with one row per taxon, a
\code{taxon} column and one column per group containing the association
value.
\item When \code{nboot_ci} is set: the list returned by \code{\link[indicspecies:strassoc]{indicspecies::strassoc()}}
with the \code{lowerCI}, \code{stat} and \code{upperCI} matrices.
}
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


A wrapper for the \code{\link[indicspecies:strassoc]{indicspecies::strassoc()}} function in the case of a
\code{physeq} object. It computes the strength of the association between each
taxon and the groups defined by \code{fact}, using one of the association
indices provided by the \code{indicspecies} package (e.g. \code{IndVal}, the phi
coefficient \code{r}, the specificity component \code{A}, or the fidelity component
\code{B}). It complements \code{\link[=multipatt_pq]{multipatt_pq()}} which tests the statistical
significance of those associations.
}
\details{
This function is mainly a wrapper of the work of others.
Please make a reference to \code{indicspecies::strassoc()} if you
use this function.
}
\examples{
\donttest{
if (requireNamespace("indicspecies")) {
  strassoc_pq(
    subset_samples(data_fungi_mini, !is.na(Height)),
    fact = "Height",
    func = "IndVal.g"
  )
}
}
\dontrun{
if (requireNamespace("indicspecies")) {
  strassoc_pq(
    subset_samples(data_fungi_mini, !is.na(Height)),
    fact = "Height",
    func = "A"
  )
  strassoc_pq(
    subset_samples(data_fungi_mini, !is.na(Height)),
    fact = "Height",
    func = "IndVal.g",
    nboot_ci = 99
  )
}
}
}
\seealso{
\code{\link[=multipatt_pq]{multipatt_pq()}}, \code{\link[indicspecies:strassoc]{indicspecies::strassoc()}}
}
\author{
Adrien Taudière
}
