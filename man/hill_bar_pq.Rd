% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{hill_bar_pq}
\alias{hill_bar_pq}
\title{Bar plot of Hill diversity with SE, jittered points, and Kruskal-Wallis test}
\usage{
hill_bar_pq(
  physeq,
  x,
  q = c(0, 2),
  fill,
  x_lab = NULL,
  y_labs = NULL,
  ncol = NULL,
  alpha = 0.6,
  point_size = 3,
  base_size = 13,
  jitter_width = 0.15,
  bar_width = 0.7,
  add_letters = TRUE,
  p_threshold = 0.05,
  letter_size = 5,
  letters_top_offset = 0.05,
  y_lab_size = NULL,
  x_lab_size = NULL,
  show_n_samples = TRUE,
  palette = c("#E69F00", "#56B4E9", "#009E73", "#F0E442", "#0072B2", "#D55E00",
    "#CC79A7", "#000000"),
  error_fun = function(x) {
     m <- mean(x, na.rm = TRUE)
     se <- sd(x, na.rm =
    TRUE)/sqrt(sum(!is.na(x)))
     c(lower = m - se, upper = m + se)
 },
  error_fun_lab = "mean ± SE",
  error_bar_alpha = 0.35,
  point_alpha = 0.5,
  letters_below_bar = FALSE,
  ...
)
}
\arguments{
\item{physeq}{(required) a \code{\link[phyloseq]{phyloseq-class}} object obtained
using the \code{phyloseq} package.}

\item{x}{Name (unquoted) of the grouping variable in \code{sam_data} (x-axis).}

\item{q}{Numeric vector of Hill diversity orders to plot. The corresponding
\verb{Hill_<q>} columns are computed by \code{\link[=psmelt_samples_pq]{psmelt_samples_pq()}}.
Default \code{c(0, 2)}.}

\item{fill}{Name (unquoted) of the fill aesthetic column. Defaults to \code{x}.}

\item{x_lab}{Label for the x-axis. Defaults to the column name of \code{x}.}

\item{y_labs}{Named character vector of y-axis labels keyed by \verb{Hill_<q>}
column name (e.g. \code{c(Hill_0 = "Richness")}). Unspecified orders receive
a default label.}

\item{ncol}{Number of columns in the patchwork layout when \code{length(q) > 1}.
Default \code{NULL} (automatic).}

\item{alpha}{Transparency of bars. Default \code{0.6}.}

\item{point_size}{Size of jittered points. Default \code{3}.}

\item{base_size}{Base font size in pts. Default \code{13}.}

\item{jitter_width}{Horizontal jitter width. Default \code{0.15}.}

\item{bar_width}{Width of bars. Default \code{0.7}.}

\item{add_letters}{Logical. Add compact letter display above bars.
Requires the \pkg{multcompView} package. Default \code{TRUE}.}

\item{p_threshold}{Significance threshold for the Kruskal-Wallis test.
Below this value, Tukey HSD pairwise comparisons are run and letters
assigned; above it all groups receive \code{"a"}. Default \code{0.05}.}

\item{letter_size}{Size of letter labels in ggplot2 units. Default \code{5}.}

\item{letters_top_offset}{Fraction of the y-range added above the highest
point / error-bar to position letters. Default \code{0.05}.}

\item{y_lab_size}{Size of y-axis tick labels in pts. Defaults to \code{base_size}.}

\item{x_lab_size}{Size of x-axis tick labels in pts. Defaults to \code{base_size}.}

\item{show_n_samples}{Logical. If \code{TRUE}, the number of samples per group is
appended below each x-axis tick label as \code{(n=X)}. Default \code{TRUE}.}

\item{palette}{Character vector of fill colours. Defaults to the Okabe-Ito
palette.}

\item{error_fun}{Function taking a numeric vector and returning a 2-element
numeric vector \code{c(lower, upper)} with the actual y-axis bounds of the
error bar (not offsets from the mean). The first element is the lower
bound, the second is the upper bound. This allows asymmetric intervals
such as quantile ranges. Default computes mean ± SE. Example for a 95\%
quantile interval: \code{function(x) quantile(x, c(0.025, 0.975), na.rm = TRUE)}.}

\item{error_fun_lab}{Label for the error bar used in the plot caption.
Default \code{"mean ± SE"}.}

\item{error_bar_alpha}{Transparency of the secondary top-half error bar
drawn over the jittered points to hint at the upper extent without
obscuring data. Default \code{0.35}.}

\item{point_alpha}{Transparency of the jittered data points. Default \code{0.5}.}

\item{letters_below_bar}{Logical. When \code{TRUE}, compact letters are placed
below the x-axis (at \code{y = -letters_top_offset * y_range}), giving a clean
fixed position independent of data spread. When \code{FALSE} (default), letters
are placed above whichever is higher: the error bar top or the highest
data point.}

\item{...}{Additional arguments passed to \code{\link[=psmelt_samples_pq]{psmelt_samples_pq()}} and hence
to \code{\link[divent:div_hill]{divent::div_hill()}} (e.g. \code{estimator = "naive"}).}
}
\value{
A \code{ggplot} object when \code{length(q) == 1}, or a \code{patchwork} object
when \code{length(q) > 1}.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


For each Hill diversity order in \code{q}, draws a bar at the group mean (±1 SE)
with jittered individual points. A Kruskal-Wallis test is reported in the
subtitle; when the global effect is significant, Tukey HSD pairwise
comparisons produce compact letter displays above the bars. Multiple values
of \code{q} are assembled into a \link[patchwork:patchwork-package]{patchwork::patchwork} layout automatically.
}
\examples{
hill_bar_pq(data_fungi_mini, Height)
\donttest{
hill_bar_pq(data_fungi_mini, Height, q = 0)
hill_bar_pq(data_fungi_mini, Height, q = c(0, 1, 2), ncol = 1)
hill_bar_pq(data_fungi_mini, Height, q = c(0, 2),
  y_labs = c(Hill_0 = "Richness", Hill_2 = "Simpson diversity"))
hill_bar_pq(data_fungi_mini, Height, add_letters = FALSE)
}

}
\seealso{
\code{\link[=hill_pq]{hill_pq()}}, \code{\link[=psmelt_samples_pq]{psmelt_samples_pq()}}, \code{\link[=ggbetween_pq]{ggbetween_pq()}}
}
\author{
Adrien Taudière
}
