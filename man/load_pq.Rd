% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dada_phyloseq.R
\name{load_pq}
\alias{load_pq}
\title{Load a phyloseq object from an RData file written by \code{save_pq()} or \code{write_pq()}}
\usage{
load_pq(path = NULL)
}
\arguments{
\item{path}{(required) a path to the folder where the phyloseq object was
saved, or directly the path to the \code{.RData}/\code{.rda} file. If a folder is
given, the file \code{physeq.RData} inside it is loaded.}
}
\value{
The loaded phyloseq object.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-maturing-blue" alt="lifecycle-maturing"></a>
}}


This is the reverse function of \code{\link[=save_pq]{save_pq()}} and \code{\link[=write_pq]{write_pq()}} when used with
\code{rdata = TRUE}. Those functions save the phyloseq object in an RData file
named \code{physeq.RData} (the object is always stored under the name \code{physeq}).
Contrary to \code{\link[base:load]{base::load()}}, \code{load_pq()} returns the phyloseq object so it can
be assigned to any name, without modifying the calling environment.
}
\examples{
\dontshow{if (tolower(Sys.info()[["sysname"]]) != "windows") withAutoprint(\{ # examplesIf}
save_pq(data_fungi, path = paste0(tempdir(), "/phyloseq"))
# Load the object and assign it to any name you like
my_pq <- load_pq(path = paste0(tempdir(), "/phyloseq"))
unlink(paste0(tempdir(), "/phyloseq"), recursive = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=save_pq]{save_pq()}}, \code{\link[=write_pq]{write_pq()}}, \code{\link[=read_pq]{read_pq()}}
}
\author{
Adrien Taudière
}
