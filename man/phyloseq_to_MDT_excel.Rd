% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phyloseq_to_MDT_excel.R
\name{phyloseq_to_MDT_excel}
\alias{phyloseq_to_MDT_excel}
\title{Export a phyloseq object to a multi-sheet Excel file for GBIF MDT submission}
\usage{
phyloseq_to_MDT_excel(
  physeq,
  filename = "Phyloseq_Tables.xlsx",
  check_dwc = TRUE
)
}
\arguments{
\item{physeq}{(required) a \code{\link[phyloseq]{phyloseq-class}} object.}

\item{filename}{(character, default \code{"Phyloseq_Tables.xlsx"}) Path of the
output \code{.xlsx} file.}

\item{check_dwc}{(logical, default TRUE) If TRUE, warn about recommended
Darwin Core sample-level terms missing from \code{sample_data}.}
}
\value{
Invisibly returns the path to the written file (\code{filename}).
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Write the OTU table, the sample data, the taxonomy table and (if present) the
reference sequences of a phyloseq object to a single multi-sheet \code{.xlsx}
file, formatted for submission to the GBIF
\href{https://mdt.gbif.org/}{Metabarcoding Data Toolkit (MDT)}.

Each sheet gets a leading \code{id} column holding the row identifiers (taxa or
samples names). The reference sequences, when available, are appended as a
\code{DNA_sequence} column of the taxonomy sheet (Darwin Core DNA-derived-data
extension term).

When \code{check_dwc = TRUE} (the default), a lightweight Darwin Core compliance
check warns about recommended sample-level terms that are missing from
\code{sample_data} (\code{decimalLatitude}, \code{decimalLongitude}, \code{eventDate}). This is
a non-blocking helper, not a full validation of the GBIF MDT template.
}
\details{
This function requires the \pkg{writexl} package. See the GBIF
\href{https://mdt.gbif.org/}{Metabarcoding Data Toolkit} for the expected
input format.
}
\examples{
\donttest{
if (requireNamespace("writexl")) {
  out <- file.path(tempdir(), "data_fungi_mini_MDT.xlsx")
  phyloseq_to_MDT_excel(clean_pq(data_fungi_mini), filename = out)
  file.exists(out)
  unlink(out)
}
}
}
\author{
Adrien Taudière
}
