% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rsearch_wrappers.R
\name{vs_uchime_ref}
\alias{vs_uchime_ref}
\title{Reference-based chimera detection with vsearch}
\usage{
vs_uchime_ref(
  fasta_input,
  database,
  nonchimeras = NULL,
  chimeras = NULL,
  sizein = TRUE,
  sizeout = TRUE,
  relabel = NULL,
  relabel_sha1 = FALSE,
  fasta_width = 0,
  sample = NULL,
  log_file = NULL,
  threads = 1,
  vsearch_options = NULL,
  tmpdir = NULL
)
}
\arguments{
\item{fasta_input}{(required) Path to the query fasta file.}

\item{database}{(required) Path to the reference database fasta file.}

\item{nonchimeras}{Path to the output fasta file of non-chimeric
sequences. A temporary file is used when \code{NULL}.}

\item{chimeras}{Path to the output fasta file of chimeric sequences.
A temporary file is used when \code{NULL}.}

\item{sizein}{(logical, default TRUE) Take \verb{;size=N} annotations of the
input into account.}

\item{sizeout}{(logical, default TRUE) Add \verb{;size=N} annotations to the
output.}

\item{relabel}{Relabel output sequences with this prefix followed by a
sequential number.}

\item{relabel_sha1}{(logical, default FALSE) Relabel output sequences
with their SHA1 digest.}

\item{fasta_width}{(integer, default 0) Width of fasta sequence lines
(0 for no wrapping).}

\item{sample}{Optional sample name annotation added to output headers.}

\item{log_file}{Path to a log file.}

\item{threads}{(integer, default 1) Number of threads.}

\item{vsearch_options}{Additional vsearch command-line options.}

\item{tmpdir}{Directory for temporary files.}
}
\value{
\code{NULL} (invisibly). The non-chimeric and chimeric sequences
are written to the \code{nonchimeras} and \code{chimeras} files, respectively.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Wrapper of \code{\link[Rsearch:vs_uchime_ref]{Rsearch::vs_uchime_ref()}} (vsearch \code{--uchime_ref}). Detect
chimeric sequences in a fasta file by comparison against a reference
database. Complements \code{\link[=chimera_detection_vs]{chimera_detection_vs()}}, which performs \emph{de novo}
detection without a reference database.
}
\examples{
\dontshow{if (requireNamespace("Rsearch", quietly = TRUE) && MiscMetabar::is_vsearch_installed()) withAutoprint(\{ # examplesIf}
vs_uchime_ref(
  system.file("extdata", "ex.fasta", package = "MiscMetabar"),
  database = system.file(
    "extdata",
    "100_sp_UNITE_sh_general_release_dynamic.fasta",
    package = "MiscMetabar"
  )
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[Rsearch:vs_uchime_ref]{Rsearch::vs_uchime_ref()}}, \code{\link[=chimera_detection_vs]{chimera_detection_vs()}},
\code{\link[=chimera_removal_vs]{chimera_removal_vs()}}
}
\author{
Adrien Taudière
}
