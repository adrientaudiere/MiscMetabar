% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mmseqs2.R
\name{assign_mmseqs2}
\alias{assign_mmseqs2}
\title{Assign taxonomy using MMseqs2 easy-taxonomy}
\usage{
assign_mmseqs2(
  database,
  physeq = NULL,
  seq2search = NULL,
  mmseqs2path = find_mmseqs2(),
  behavior = c("return_matrix", "add_to_phyloseq"),
  suffix = "_mmseqs2",
  lca_mode = 3,
  lca_ranks = c("superkingdom", "phylum", "class", "order", "family", "genus", "species"),
  column_names = c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species"),
  search_type = 3,
  sensitivity = NULL,
  min_seq_id = NULL,
  e_value = NULL,
  nproc = 1,
  clean_pq = TRUE,
  simplify_taxo = TRUE,
  keep_temporary_files = FALSE,
  verbose = FALSE,
  cmd_args = ""
)
}
\arguments{
\item{database}{Path to a pre-built MMseqs2 database with taxonomy
information (created via \verb{mmseqs createdb} + \verb{mmseqs createtaxdb},
or downloaded with \verb{mmseqs databases}).}

\item{physeq}{(required) a \code{\link[phyloseq]{phyloseq-class}} object obtained
using the \code{phyloseq} package.}

\item{seq2search}{(optional) A \link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet} object. Use
instead of \code{physeq} to search arbitrary sequences. Cannot be used
together with \code{physeq}.}

\item{mmseqs2path}{Path to the \code{mmseqs} binary
(default: \code{\link[=find_mmseqs2]{find_mmseqs2()}}).}

\item{behavior}{Either \code{"return_matrix"} (default) or
\code{"add_to_phyloseq"}:
\itemize{
\item \code{"return_matrix"}: return a data frame with taxonomic assignments.
\item \code{"add_to_phyloseq"}: return a phyloseq object with the
taxonomy appended to the \code{tax_table} slot.
}}

\item{suffix}{(character) Suffix appended to new taxonomy column names
(default: \code{"_mmseqs2"}).}

\item{lca_mode}{(integer) The LCA mode used by MMseqs2:
\itemize{
\item \code{1}: single search LCA
\item \code{3} (default): approximate 2bLCA (fast, recommended)
\item \code{4}: top-hit LCA (all equal-scoring top hits)
}}

\item{lca_ranks}{Character vector of NCBI taxonomy rank names passed
to \code{--lca-ranks} (default:
\code{c("superkingdom", "phylum", "class", "order", "family", "genus", "species")}).}

\item{column_names}{Character vector of output column names, must be
the same length as \code{lca_ranks} (default:
\code{c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species")}).}

\item{search_type}{(integer) MMseqs2 search type:
\itemize{
\item \code{0}: auto-detect
\item \code{2}: translated nucleotide
\item \code{3} (default): nucleotide
}}

\item{sensitivity}{(numeric, optional) Search sensitivity (\code{-s}
parameter). Higher values are slower but more sensitive (range 1--7).
If \code{NULL}, MMseqs2 uses its default.}

\item{min_seq_id}{(numeric, optional) Minimum sequence identity
(0--1). If \code{NULL}, MMseqs2 uses its default.}

\item{e_value}{(numeric, optional) Maximum E-value threshold.
If \code{NULL}, MMseqs2 uses its default.}

\item{nproc}{(integer) Number of threads (default: 1).}

\item{clean_pq}{(logical) Clean the phyloseq object before
searching? (default: \code{TRUE}).}

\item{simplify_taxo}{(logical) Apply \code{\link[=simplify_taxo]{simplify_taxo()}} to the
result? Only used when \code{behavior = "add_to_phyloseq"} (default:
\code{TRUE}).}

\item{keep_temporary_files}{(logical) Keep intermediate files
for debugging? (default: \code{FALSE}).}

\item{verbose}{(logical) Print progress messages? (default: \code{FALSE}).}

\item{cmd_args}{(character) Additional arguments appended to the
\verb{mmseqs easy-taxonomy} call.}
}
\value{
\itemize{
\item If \code{behavior == "return_matrix"}: a \link[tibble:tibble]{tibble} with
columns \code{taxa_names} and one column per rank.
\item If \code{behavior == "add_to_phyloseq"}: a new phyloseq object with
amended \code{tax_table}.
}
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Use the \href{https://github.com/soedinglab/MMseqs2}{MMseqs2} \code{easy-taxonomy}
workflow to assign taxonomy via Lowest Common Ancestor (LCA) against a
pre-built MMseqs2 sequence database with taxonomy information.

The database must be created beforehand using \verb{mmseqs createdb} +
\verb{mmseqs createtaxdb}, or downloaded with \verb{mmseqs databases}
(e.g. \verb{mmseqs databases SILVA silvadb tmp}). See the
\href{https://github.com/soedinglab/MMseqs2/wiki}{MMseqs2 wiki} for details.
}
\details{
This function is mainly a wrapper of the work of others.
Please cite \href{https://github.com/soedinglab/MMseqs2}{MMseqs2}:
Mirdita M, Steinegger M, Breitwieser F, Soding J, Levy Karin E:
Fast and sensitive taxonomic assignment to metagenomic contigs.
Bioinformatics (2021).
}
\examples{
\dontrun{
# First, create a MMseqs2 database with taxonomy:
# mmseqs createdb ref_seqs.fasta refDB
# mmseqs createtaxdb refDB tmp --ncbi-tax-dump taxonomy/ \
#   --tax-mapping-file mapping.tsv

# Or download a pre-built database:
# mmseqs databases SILVA silvadb tmp

# Then assign taxonomy:
res <- assign_mmseqs2(
  data_fungi_mini,
  database = "/path/to/mmseqs2db"
)

# Add to phyloseq:
physeq_new <- assign_mmseqs2(
  data_fungi_mini,
  database = "/path/to/mmseqs2db",
  behavior = "add_to_phyloseq"
)
}
}
\seealso{
\code{\link[=assign_blastn]{assign_blastn()}}, \code{\link[=assign_sintax]{assign_sintax()}}, \code{\link[=assign_vsearch_lca]{assign_vsearch_lca()}}
}
\author{
Adrien Taudière
}
