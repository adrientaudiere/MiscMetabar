I want to add a new test package to test for function that use "fact" param in the case of physeq object with only one sample

---

In the case of a factor with only one level (for exemple if there is only one samples), I want that the functions work. So lets create the test file in a new testthat file and after we will improve the code to pass the test (sort of test-driven development).

---

[Request interrupted by user for tool use]