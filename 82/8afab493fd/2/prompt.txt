Base directory for this skill: /home/adrien/.claude/skills/r-feature-batch

Implement a batch of **3-4 ROADMAP features** for a pqverse R package, **one feature at a
time** in the `dev` working tree, then finish with **one consolidation pass** that does every
global edit exactly once and **one Quarto demo** illustrating all the new features together.

This skill reproduces the workflow proven in MiscMetabar 0.16.8.9000 (clean_pq NA-cleanup,
reshape_ggplot, phyloseq_to_MDT_excel, fastp, tool-presence checks).

## The discipline that keeps the diff clean

While implementing each feature, touch **only its own files** — `R/<slug>.R` and
`tests/testthat/test_<slug>.R`. Defer **every global file** to the single consolidation stage
at the end:

`NEWS.md`, `_pkgdown.yml`, `DESCRIPTION` (Suggests), `inst/WORDLIST`, `NAMESPACE`, `man/`.

Why batch them: NEWS bullets are alphabetised across the *whole* set at once; `document()`
and `air format` run once over the final tree (not N times); and a single `git diff
NAMESPACE man/` at the end cleanly reveals any carryover. Editing globals per-feature just
creates churn and re-sorting work.

## Arguments

`` may contain, in any order:

- A count (`3` or `4`) — how many features to implement. If absent, ask.
- `--filter "High/easy"` — priority/facility filter for candidate selection. Default:
  `High/easy` plus `Medium/easy`. Prefer items carrying a `(source: …)` prototype pointer.
- `--package <name>` — target package. If absent, read `Package:` from `DESCRIPTION` in the
  cwd. Resolves to `pqverse_pkg/<name>/` from the workspace root.

## Commits

**Leave everything uncommitted on `dev` for the developer.** The rule is "Don't commit, the
developer do." This skill never commits, branches, or merges.

---

## Stage 0 — Preconditions & context

1. Confirm cwd (or `--package` target) is an R package: `DESCRIPTION` present. Read `Package:`.
2. Locate the workspace root and `ROADMAP.md` (pqverse root, e.g.
   `/home/adrien/Nextcloud/IdEst/Projets/pqverse/ROADMAP.md`).
3. Confirm the current branch is `dev` (or ask). Run `git status` so you know the starting
   diff and can keep the new work scoped.
4. Identify the demo target dir: `<workspace>/arround_MiscMetabar/`.

## Stage 1 — Select features

1. Read the target package's section of `ROADMAP.md`. Extract bullets matching the filter.
   **Strongly prefer items with a `(source: …)` pointer** — the prototype already exists, so
   they're lower-risk and faster. Open each pointer (often
   `arround_MiscMetabar/en_cours_de_dev.qmd:<lines>`) to confirm scope.
2. Present the candidates to the user with `AskUserQuestion` (`multiSelect: true`). Let them
   pick the 3-4 to build. Show the one-line description and facility tag per option.
3. For each chosen feature, record a small manifest:
   - `slug` — kebab-case, drives `R/<slug>.R` and `test_<slug>.R`.
   - target function name(s) (with `()`),
   - one-line description + source pointer,
   - the ROADMAP bullet text (to delete from ROADMAP on success — mirror the prior session).

## Stage 2 — Implement each feature (linear, one at a time)

For each selected feature, in turn:

1. **Read 2-3 existing functions** in `R/` to match conventions before writing anything.
2. Write `R/<slug>.R`:
   - Base pipe `|>` (never `%>%`); `function() {}` not `\()`; `{}` around every if/else body.
   - Full roxygen2 (`@param`, `@return`, `@export`, `@examples`, `@author`), matching the
     style of neighbouring functions.
   - Guard every Suggests-package call with `requireNamespace("<pkg>", quietly = TRUE)` +
     `cli::cli_abort(...)`. **Note** any new Suggests dependency for Stage 3 — do **not**
     edit `DESCRIPTION` yet.
   - CRAN constraints: primary example in `\donttest{}`, variants in `\dontrun{}`; cap any
     per-sample loop at 5 samples via
     `prune_samples(sample_names(data_fungi_mini)[1:5], data_fungi_mini)`
     (`data_fungi_mini` has 137 samples); external-tool examples in `\dontrun{}`.
3. Write `tests/testthat/test_<slug>.R`:
   - **Underscore** filename — repo convention. (r_development.md says `test-<name>.R` with a
     hyphen; that's wrong for this repo. Use underscore.)
   - `skip_on_cran()` as the **first line** if the whole file should skip on CRAN.
   - Minimal, few comments. Guard Suggests-dependent tests with
     `skip_if_not_installed("<pkg>")`.
4. **Validate this feature before moving on:**
   `NOT_CRAN=true Rscript -e "devtools::load_all('.'); testthat::test_file('tests/testthat/test_<slug>.R')"`
   (never append `2>&1` to Rscript calls). Don't start the next feature with this one red.

Do **not** edit any global file in this stage — collect the per-feature notes (function
names, new Suggests deps, NEWS bullet text, `_pkgdown.yml` section, spelling terms) for
Stage 3.

## Stage 3 — Consolidate (ONE pass over the global files)

Once all features are implemented and individually green, do the global edits once, in order:

1. **NEWS.md** — add every bullet, then **re-sort the whole current-version list**
   alphabetically by function name (bullets without a function name go first). Present tense,
   function name in `backticks()` early in the bullet.
2. **_pkgdown.yml** — add each new topic to the section it belongs in.
3. **DESCRIPTION** — add all new Suggests deps in a single edit, keep the list sorted.
4. **inst/WORDLIST** — add all flagged terms; keep it `LC_ALL=C` sorted, en-US.
5. **`air format`** — run, then **scope the diff**: `air format .` reformats unrelated files.
   Revert incidental reformats so the diff stays feature-scoped
   (`git checkout -- <unrelated.R>`), or format only the changed files.
6. **`devtools::document()` ONCE** → regenerates `NAMESPACE` + `man/` with all new exports
   together.
7. **Carryover check** — `git diff NAMESPACE man/` and flag anything **not** attributable to
   the new features (e.g. a prior commit that never ran `document()`, like the
   `rarefy_even_depth_pq` carryover). Report it; don't silently bundle it.
8. **Spelling** — `Rscript -e "spelling::spell_check_package()"` once; fix real typos, add
   legitimate jargon to `inst/WORDLIST`.
9. **Full test suite** — once, `NOT_CRAN=true Rscript -e "devtools::test()"`. Swallow output;
   show `✓` if green, dump on failure.

## Stage 4 — One Quarto demo for all features

Write `<workspace>/arround_MiscMetabar/<name>_demo.qmd` covering **every** new feature in one
document:

- A setup chunk that `devtools::load_all()`s the dev package and loads
  `data(data_fungi_mini)`, capped to 5 samples for any per-sample work.
- One section per feature: a short prose explanation + a **runnable** chunk. Tool-dependent
  features (fastp etc.) use the dry-run / `cmd_is_run = FALSE` path or `is_*_installed()` so
  the chunk evaluates without the external binary.
- Close with `sessionInfo()`.
- **Verify the chunks actually evaluate** (render, or source each chunk) against the
  5-sample subset — a demo that only *looks* runnable is a regression waiting to happen.

## Stage 5 — ROADMAP cleanup & report

1. **ROADMAP.md** — delete the shipped bullets and any now-dead `(source:)` pointer rows. If a
   prototype in `en_cours_de_dev.qmd` is fully shipped, offer to remove that section too.
2. **Report** a table of features shipped (function · test · NEWS · pkgdown · man), the test
   result, spelling status, the demo path, and any `NAMESPACE`/`man/` **carryover** flagged
   in Stage 3.7. State plainly that the diff is left **uncommitted on `dev`** for the
   developer, and that **R CMD check was not run** — offer `/r-check`.

## Important rules

- **Never commit** — the developer commits.
- **No global-file edits while implementing a feature.** All of NEWS / _pkgdown / DESCRIPTION
  / WORDLIST / NAMESPACE / man happen once, in Stage 3.
- **When scope or design is ambiguous, ask the developer** — especially before writing a
  custom implementation of something a CRAN package (vegan, etc.) likely already provides.
- Tests are `test_<slug>.R` (underscore). Never append `2>&1` to Rscript Bash calls.
- Prefer items with an existing `(source:)` prototype; they're the low-risk path.

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
1. Primary Request and Intent:
   - **Original:** User invoked `/r-feature-batch` to implement 3-4 ROADMAP features for the MiscMetabar R package, one at a time on the `dev` branch, then consolidate all global-file edits once, write a single Quarto demo, and leave everything uncommitted for the developer. The skill explicitly forbids committing.
   - **Feature selection:** User chose 3 features: (1) Volcano plot for differential abundance → `plot_volcano_pq()`, (2) `plot_overview_pq()` single-call alpha+beta overview, (3) GBIF MDT CSV export → `phyloseq_to_MDT_csv()`.
   - **Interrupt 1:** "show me the html demo file in a browser" (completed).
   - **Interrupt 2 (CURRENT, overriding):** "The plot_overview_pq is not what I want lets talk about how to implement this function and plan a new version." → User wants to REDESIGN `plot_overview_pq()` collaboratively.
   - **Most recent signal:** User rejected my reformulated AskUserQuestion, saying they want to clarify the questions first ("Start by asking them what they would like to clarify"). User pre-answered Q1 Core panels as: "Taxonomic composition, Alpha diversity, Beta ordination, venn diagramm (or upset plot)" — notably ADDING a venn/upset panel that was not among my offered options.

2. Key Technical Concepts:
   - R package development for phyloseq objects (pqverse ecosystem: MiscMetabar, tidypq, etc.)
   - phyloseq S4 object slots: otu_table, tax_table, sam_data, refseq
   - CRAN check-time constraints: data_fungi_mini has 137 samples; cap per-sample-loop examples to 5; `\donttest{}` runs on CRAN, `\dontrun{}` never runs; dada2/purrr/divent in Imports not Depends (need `library()` in test files)
   - divent in Imports → produces "CheckArguments because divent is not attached" warnings (documented, harmless)
   - Hill numbers (q=0,1,2) via hill_pq(); ordination via plot_ordination_pq() (PCoA recommended over NMDS for small data); patchwork for composition
   - Differential abundance: DESeq2 (log2FoldChange/padj), ANCOMBC (lfc_*/q_*), ALDEx2 (effect/wi.eBH)
   - GBIF MDT template: 4 sheets (OTU_table, Taxonomy, Samples, Study); OTU table = OTU IDs in ROWS, sample IDs in COLUMNS; tables keyed by `id` column; accepts TSV+XLSX
   - Conventions: base pipe `|>`, `function() {}` not `\()`, `{}` around all if/else, en-US spelling, `air format`, roxygen2, NEWS bullets alphabetical by function name, underscore test filenames `test_<slug>.R`, lifecycle-experimental badge in roxygen
   - RTK hook rewrites bash commands (rg→grep can break with directories)

3. Files and Code Sections:
   - **`R/plot_volcano_pq.R`** (CREATED, 9 tests PASS) — Volcano plot, column-agnostic. Signature: `plot_volcano_pq(df, fc = "log2FoldChange", padj = "padj", alpha = 0.05, lfc_threshold = 1, label_col = NULL, label_n = 10, point_size = 2, point_alpha = 0.7, palette = c(Down = "#1d2949", NotDA = "grey70", Up = "#4B3E1E"))`. Handles padj==0 (caps -log10 at finite_max*1.05) and NA padj (→NotDA). Status factor levels c("Down","NotDA","Up"). Uses `is_signif` (renamed from `signif` to avoid base-symbol lint). Pure ggplot2.
   - **`R/plot_overview_pq.R`** (CREATED — USER WANTS REDESIGNED). Current signature: `plot_overview_pq(physeq, fact, q = c(0, 1, 2), ordination_method = "PCoA", dist_method = "bray", add_alpha = TRUE, add_ordination = TRUE, ...)`. Currently just composes `hill_pq(physeq, fact=fact, q=q, one_plot=FALSE, plot_with_tuckey=FALSE)` (returns named list of ggplots) + `plot_ordination_pq(physeq, method=dist_method, ordination_method=ordination_method, color=fact)` via `patchwork::wrap_plots(plots, ...)`. Guards patchwork with requireNamespace+cli_abort. **This is the file to be rewritten per the new design.**
   - **`R/phyloseq_to_MDT_csv.R`** (CREATED, 8 tests PASS). Signature: `phyloseq_to_MDT_csv(physeq, path = ".", prefix = "", sep = ",", check_dwc = TRUE)`. Writes 3 files (OTU_table, Taxonomy, Samples) via internal `write_one()` helper (uses `out_file`, not `file`, to avoid lint). **Correct MDT orientation: taxa in rows, samples in columns, leading `id` column** (opposite of the buggy excel exporter). ext .csv for sep="," else .tsv. Returns invisible named char vector. DWC check warns on missing decimalLatitude/Longitude/eventDate.
   - **`R/phyloseq_to_MDT_excel.R`** (READ ONLY, template) — Has the WRONG orientation bug: writes samples-in-rows. NOT modified (flagged for developer).
   - **Test files** (CREATED): `tests/testthat/test_plot_volcano_pq.R`, `test_plot_overview_pq.R`, `test_phyloseq_to_MDT_csv.R` (underscore convention, skip_on_cran, skip_if_not_installed guards).
   - **`NEWS.md`** (MODIFIED) — under `# MiscMetabar 0.16.8.9000`, added 3 bullets, re-sorted with "Rarefaction across the package..." (non-function) first, then alphabetical: clean_pq, fastp, is_blastn_installed, phyloseq_to_MDT_csv, phyloseq_to_MDT_excel, plot_overview_pq, plot_volcano_pq, rarefy_pq, reshape_ggplot.
   - **`_pkgdown.yml`** (MODIFIED) — plot_overview_pq after plot_ordination_pq (beta section); plot_volcano_pq after plot_mt (DA section); phyloseq_to_MDT_csv before phyloseq_to_MDT_excel (Import/export).
   - **`inst/WORDLIST`** (MODIFIED) — added TSV, XLSX, validator (LC_ALL=C re-sorted).
   - **`ROADMAP.md`** (MODIFIED, at pqverse root) — deleted 3 shipped bullets (volcano, plot_overview_pq, GBIF MDT CSV).
   - **`arround_MiscMetabar/overview_volcano_mdt_demo.qmd`** (CREATED) + **`overview_volcano_mdt_demo.html`** (RENDERED, 1.9MB, untracked artifact) — demo covering all 3 features; setup loads dev pkg via devtools::load_all, uses 6-sample subset (3 High + 3 Low).
   - **`.claude/settings.json`** (MODIFIED during session, NOT by me) — harness-written permission update: added Bash(cd/tail/head/ps/which/rtk rg) to allow, removed Read(./.entire/metadata/**) deny.
   - **`summary_plot_pq()`** in R/plot_functions.R:2179 (READ) — existing STRUCTURAL overview (shows slots otu_table/tax_table/sam_data/refseq with counts), NOT a diversity overview; does not collide with plot_overview_pq's diversity purpose.

4. Errors and fixes:
   - **RTK hook rewrote `rg`→`grep`** causing "Is a directory" errors → switched to `grep -r`.
   - **Lint: reused base symbol `signif`** in plot_volcano_pq.R → renamed to `is_signif`.
   - **Lint: reused base symbol `file`** in phyloseq_to_MDT_csv.R write_one() → renamed to `out_file`.
   - **plot_overview_pq test with q=0 failed** (`dim(X) must have a positive length` — pre-existing hill_pq limitation: single-q drops to vector) → changed test to q=c(0,1). Documented q as a vector.
   - **MDT format: I almost guessed** → advisor said WebFetch the docs. WebFetch 403/404, so WebSearch confirmed correct orientation (taxa-in-rows).
   - **British spellings** (coloured/colouring/favoured) flagged by spell check → changed to American (Language=en-US, codebase uses color 235x vs colour 14x).
   - **`.claude/settings.json` mis-attribution:** I initially said it was "pre-existing"; advisor corrected me — it was modified DURING the session. Verified via git diff: it's a harness permission-allowlist write (not my deliverable, not mine to revert).
   - **AskUserQuestion >4 options rejected** → split 6 panel options into two 4-and-3-option questions.
   - **User REJECTED the reformulated AskUserQuestion**, wanting to clarify the questions first. User's instruction: "The user wants to clarify these questions... Start by asking them what they would like to clarify."

5. Problem Solving:
   - All 3 features implemented, individually validated (9+3+8=20 PASS), consolidated cleanly (no NAMESPACE/man carryover), spelling clean, full suite exit 0, demo renders end-to-end.
   - Identified and flagged (NOT fixed) the `phyloseq_to_MDT_excel` orientation bug — built CSV exporter to correct orientation, left excel for developer decision.
   - The feature batch was essentially complete when the user pivoted to wanting plot_overview_pq redesigned.

6. All user messages:
   - [/clear then /r-feature-batch skill invocation with no args]
   - (via AskUserQuestion answer) Selected features: "Volcano plot for DA, plot_overview_pq(), GBIF MDT CSV export"
   - "show me the html demo file in a browser"
   - "The plot_overview_pq is not what I want lets talk about how to implement this function and plan a new version."
   - (via AskUserQuestion rejection) Answered Core panels: "Taxonomic composition, Alpha diversity, Beta ordination, venn diagramm (or upset plot"; left Q2-Q4 unanswered; rejection note: "The user wants to clarify these questions. This means they may have additional information, context or questions for you. Take their response into account and then reformulate the questions if appropriate. Start by asking them what they would like to clarify."

7. Pending Tasks:
   - **PRIMARY (active):** Redesign/plan a new version of `plot_overview_pq()`. User wants it to include (at minimum): Taxonomic composition, Alpha diversity, Beta ordination, AND a Venn diagram or Upset plot panel (note: venn/upset was user-added, not in my original options). Grouping-factor requirement, extra panels (rarefaction, prevalence), and return type are still undecided.
   - **Immediate sub-task per user instruction:** ASK the user what they'd like to clarify about the design questions before reformulating them.
   - (Batch leftover, not yet reported to user): final feature-batch report (3 features shipped, tests green, demo path, MDT orientation discrepancy flag, R CMD check NOT run → offer /r-check). This was about to be delivered when the user pivoted.

8. Current Work:
   Immediately before this summary, I was redesigning `plot_overview_pq()` per user interrupt 2. I had explained the current implementation and nearby existing functions (summary_plot_pq, hill_pq, plot_ordination_pq, tax_bar_pq/plot_tax_pq, SRS_curve_pq, hill_curves_pq, venn_pq, ggvenn_pq, upset_pq), then attempted an AskUserQuestion with 4 questions (Core panels, Extra panels, Factor required/optional, Return type). The user REJECTED it, pre-answering only Core panels as "Taxonomic composition, Alpha diversity, Beta ordination, venn diagramm (or upset plot)" and instructing me to first ask them what they'd like to clarify about the questions. The MiscMetabar functions relevant to the user's venn/upset addition are `venn_pq()`, `ggvenn_pq()`, and `upset_pq()` (all confirmed to exist; ggvenn_pq/upset_pq are in the beta-diversity pkgdown section).

9. Optional Next Step:
   Respond in plain text (no AskUserQuestion yet) asking the user what they would like to clarify about the design questions, per their explicit instruction: "Start by asking them what they would like to clarify." I should acknowledge their Core-panels answer (Taxonomic composition, Alpha diversity, Beta ordination, + Venn/Upset diagram — noting venn/upset is a new addition I'll incorporate) and invite them to tell me what about the remaining questions (extra panels, factor required/optional, return type) or the panel set itself they want to adjust or discuss further, before I reformulate. Do NOT write any code for the redesign until the design is settled with the user.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /home/adrien/.claude/projects/-home-adrien-Nextcloud-IdEst-Projets-pqverse-pqverse-pkg-MiscMetabar/8c8247cc-e0a5-4297-93cc-2a0c9a87b8c6.jsonl
Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.

---

Continue from where you left off.

---

The plot_overview_pq is not what I want lets talk about how to implement this function and plan a new version.

    - l'idée avoir les principaux types graphiques pertinents de alpha et beta-div en fonction d'une seul modalité
    - faire des cas en fonction de la modalité (numérique, facteur à 2 niveaux, 3 niveaux,  + de niveaux )
    - venn/upset plot, alpha div de hill (quelle fonction), umap
    - à compléter avec une fonction qui ferait plus un overview de la "qualité" de l'objet phyloseq, c-a-d un diagnostique (taxa with low frequency or low abundance; low nb seq/nbtaxa by sample, controls ?)

---

in plot_overview I prefer for alpha diversity the use of function hill_bar_pq for factor and MiscMetabar::ggscatt_pq() for numeric modality.